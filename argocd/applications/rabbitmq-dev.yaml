apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: rabbitmq-dev
  namespace: argocd
spec:
  project: amy

  destination:
    server: https://kubernetes.default.svc
    namespace: dev

  source:
    repoURL: https://github.com/amolsurjuse/k8s-platform.git
    targetRevision: develop
    path: infrastructure/rabbitmq
    helm:
      releaseName: rabbitmq
      valueFiles:
        - ../../platform-config/infrastructure/rabbitmq/us/values/dev.yaml

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
