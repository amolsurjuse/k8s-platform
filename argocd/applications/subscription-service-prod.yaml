apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: subscription-service-prod
  namespace: argocd
spec:
  project: electrahub-prod
  destination:
    server: https://kubernetes.default.svc
    namespace: prod
  source:
    repoURL: https://github.com/amolsurjuse/k8s-platform.git
    targetRevision: develop
    path: charts/common
    helm:
      releaseName: subscription-service
      valueFiles:
        - subscription-service/values.yaml
        - ../config/services/subscription-service/us/base.yaml
        - ../config/services/subscription-service/us/values/prod-values.yaml
        - ../config/services/subscription-service/us/version/prod-version.yaml
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
