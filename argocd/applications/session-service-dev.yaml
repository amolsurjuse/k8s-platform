apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: session-service
  namespace: argocd
spec:
  project: amy
  destination:
    server: https://kubernetes.default.svc
    namespace: dev
  source:
    repoURL: https://github.com/amolsurjuse/k8s-platform.git
    targetRevision: develop
    path: charts/common
    helm:
      releaseName: session-service
      valueFiles:
        - session-service/values.yaml
        - ../config/services/session-service/us/base.yaml
        - ../config/services/session-service/us/values/dev-values.yaml
        - ../config/services/session-service/us/version/dev-version.yaml
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
