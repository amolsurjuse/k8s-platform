apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: charger-management-service
  namespace: argocd
spec:
  project: amy
  destination:
    server: https://kubernetes.default.svc
    namespace: dev
  source:
    repoURL: https://github.com/amolsurjuse/k8s-platform.git
    targetRevision: develop
    path: charts/common
    helm:
      releaseName: charger-management-service
      valueFiles:
        - charger-management-service/values.yaml
        - ../config/services/charger-management-service/us/values/dev-values.yaml
        - ../config/services/charger-management-service/us/version/dev-version.yaml
        - ../config/services/charger-management-service/us/base.yaml
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
