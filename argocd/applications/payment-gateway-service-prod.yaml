apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: payment-gateway-service-prod
  namespace: argocd
spec:
  project: electrahub-prod
  destination:
    server: https://kubernetes.default.svc
    namespace: prod
  source:
    repoURL: https://github.com/amolsurjuse/k8s-platform.git
    targetRevision: develop
    path: charts/common
    helm:
      releaseName: payment-gateway-service
      valueFiles:
        - payment-gateway-service/values.yaml
        - ../config/services/payment-gateway-service/us/base.yaml
        - ../config/services/payment-gateway-service/us/values/prod-values.yaml
        - ../config/services/payment-gateway-service/us/version/prod-version.yaml
  syncPolicy:
    # Temporary release gate: removed again after the tagged production rollout is healthy.
    automated:
      prune: false
      selfHeal: false
    syncOptions:
      - CreateNamespace=true
