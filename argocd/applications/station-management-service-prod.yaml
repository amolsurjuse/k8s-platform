apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: station-management-service-prod
  namespace: argocd
spec:
  project: electrahub-prod
  destination:
    server: https://kubernetes.default.svc
    namespace: prod
  source:
    repoURL: https://github.com/amolsurjuse/k8s-platform.git
    targetRevision: develop
    path: charts/common
    helm:
      releaseName: station-management-service
      valueFiles:
        - station-management-service/values.yaml
        - ../config/services/station-management-service/us/base.yaml
        - ../config/services/station-management-service/us/values/prod-values.yaml
        - ../config/services/station-management-service/us/version/prod-version.yaml
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
