apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: session-service-prod
  namespace: argocd
spec:
  project: electrahub-prod
  destination:
    server: https://kubernetes.default.svc
    namespace: prod
  source:
    repoURL: https://github.com/amolsurjuse/k8s-platform.git
    targetRevision: develop
    path: charts/common
    helm:
      releaseName: session-service
      valueFiles:
        - session-service/values.yaml
        - ../config/services/session-service/us/base.yaml
        - ../config/services/session-service/us/prod.yaml
        - ../config/services/session-service/us/version/prod-version.yaml
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
