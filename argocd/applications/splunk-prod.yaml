apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: splunk-prod
  namespace: argocd
spec:
  project: electrahub-prod

  destination:
    server: https://kubernetes.default.svc
    namespace: prod

  source:
    repoURL: https://github.com/amolsurjuse/k8s-platform.git
    targetRevision: develop
    path: infrastructure/splunk
    helm:
      releaseName: splunk
      valueFiles:
        - ../../platform-config/infrastructure/splunk/us/values/prod.yaml

  syncPolicy:
    syncOptions:
      - CreateNamespace=true
