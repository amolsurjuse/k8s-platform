apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.fullnameOverride }}
spec:
  selector:
    app.kubernetes.io/name: {{ .Values.fullnameOverride }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
