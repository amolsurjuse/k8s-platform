{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ default (include "prometheus.name" .) .Values.serviceAccount.name }}
  labels:
    {{- include "prometheus.labels" . | nindent 4 }}
{{- end }}
