fullnameOverride: elasticsearch

replicaCount: 1

image:
  repository: docker.elastic.co/elasticsearch/elasticsearch
  tag: "8.13.4"

esConfig:
  clusterName: electrahub-es-dev
  javaOpts: "-Xms512m -Xmx512m"
  singleNode: true
  xpackSecurityEnabled: false

persistence:
  enabled: true
  size: 10Gi

resources:
  requests:
    cpu: 500m
    memory: 1Gi
  limits:
    cpu: "1"
    memory: 2Gi

pdb:
  enabled: false
