targetNamespace: prod
clusterName: electrahub-k3d
environment: prod

splunk:
  endpoint: https://splunk.prod.svc.cluster.local:8088
  index: main
  sourceType: kubernetes:container

elasticsearch:
  enabled: false
  # Application Elasticsearch is reserved for session, pricing, and billing queries.
  # Enable this only after the dedicated observability Elasticsearch endpoint exists.
  endpoint: http://elasticsearch-logs.prod.svc.cluster.local:9200
  index: electrahub-kubernetes-%Y.%m.%d
